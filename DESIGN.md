# ReviewFlow Desktop 디자인 시스템

## 0. Research Log

- 방향: 업무용 데이터 대시보드와 고객 응대 작업공간을 결합한 밝은 데스크톱 UI
- 내장 참고: minimalist-skill의 절제와 Linear의 정보 밀도, 상태 표현, 계층 구조를 참고했다.
- 실제 제품 참고: Front, Intercom, Threado의 목록과 상세 패널 구조를 참고했다.
- 제외: 브랜드 색상, 로고, 문구는 복제하지 않는다. 장식용 애니메이션과 과한 그림자는 사용하지 않는다.
- 도구 제약: 별도 이미지 시안은 만들지 않고 승인된 shadcn 스타일을 구현 계약으로 사용한다.

## 1. 방향

운영자가 많은 후기를 빠르게 훑고 실패 항목을 다시 처리할 수 있는 조용한 업무 도구다. 따뜻한 회백색 캔버스 위에 선명한 흰색 패널과 얇은 테두리를 두고, 청록색은 주요 동작과 선택 상태에만 사용한다.

## 2. 사용자와 주요 작업

- 온라인 판매자: 후기 여러 건을 한 번에 분석하고 답변 후보를 복사한다.
- 고객 응대 실무자: 부정 후기와 실패 항목을 먼저 찾고 적절한 답변을 비교한다.
- 키보드 사용자: 프로그램 선택부터 분석 시작, 결과 이동, 복사까지 마우스 없이 수행한다.
- 저시력 사용자: 4.5:1 이상의 대비, 텍스트 상태 라벨, 200% 확대에서도 정보 손실 없는 흐름이 필요하다.

## 3. 토큰

### 색상

| 토큰 | 값 | 용도 |
| --- | --- | --- |
| `canvas` | `#F5F4F0` | 앱 배경 |
| `surface` | `#FFFFFF` | 패널과 카드 |
| `surface-muted` | `#F8F8F6` | 보조 영역 |
| `text` | `#18201D` | 제목과 본문 |
| `text-muted` | `#5E6863` | 보조 설명 |
| `border` | `#D9DDD9` | 기본 테두리 |
| `accent` | `#0F766E` | 주요 버튼과 선택 상태 |
| `accent-strong` | `#115E59` | hover 상태 |
| `danger` | `#B42318` | 오류와 실패 |
| `warning` | `#9A6700` | 주의와 대기 |
| `success` | `#26734D` | 성공 |

색상은 상태의 유일한 표현 수단으로 사용하지 않는다. 항상 아이콘 또는 텍스트 라벨을 함께 표시한다.

### 글꼴과 크기

- 기본: Pretendard, Apple SD Gothic Neo, system-ui, sans-serif
- 화면 제목: 24px, 700
- 섹션 제목: 16px, 650
- 본문: 14px, 400, 줄높이 1.55
- 보조 텍스트: 12px, 500
- 버튼과 입력: 14px, 600

### 간격과 형태

- 간격 단위: 4px, 주요 리듬은 8px, 12px, 16px, 24px, 32px
- 버튼과 입력 반경: 8px
- 카드와 패널 반경: 10px
- 클릭 영역 최소 높이: 44px
- 그림자: 팝오버에만 약하게 사용하고 기본 패널은 테두리로 구분한다.

## 4. 레이아웃

- 앱 셸: 최소 높이 `100dvh`, 상단 헤더와 독립 스크롤 본문
- 콘텐츠 최대 폭: 1440px, 좌우 여백 24px, 모바일 16px
- 런처: 데스크톱 3열, 태블릿 2열, 모바일 1열 프로그램 카드
- 분석 화면: 데스크톱에서 입력 패널과 결과 패널의 5:7 분할, 1024px 아래는 한 열
- 결과 상세: 선택한 후기의 분석과 답변 후보를 같은 화면에서 비교한다.
- 목록은 긴 단어와 한글 줄바꿈을 안전하게 처리하고 가로 스크롤을 만들지 않는다.

## 5. 공용 프리미티브와 상태

### Button

- 변형: primary, secondary, ghost, danger
- 상태: default, hover, focus-visible, disabled, loading
- 로딩 중 `cursor: wait`, disabled는 `cursor: not-allowed`, 나머지는 `cursor: pointer`

### Input, Textarea, Select

- 연결된 label과 보조 설명, 오류 메시지를 제공한다.
- focus-visible에서 2px accent 링을 표시한다.
- 오류는 테두리와 텍스트를 함께 사용한다.

### ProgramCard

- 아이콘, 이름, 설명, 버전, 상태를 표시한다.
- `available`만 실행 가능하며 `coming_soon`은 이유를 텍스트로 표시한다.

### StatusBadge

- queued, running, succeeded, failed, cancelled와 감성 상태를 텍스트로 표시한다.
- 아이콘 크기는 16px로 통일한다.

### ReviewRow와 ReviewDetail

- 원문, 상태, 감성, 요약을 빠르게 훑을 수 있다.
- 선택 상태는 배경색과 좌측 강조선, `aria-current`로 함께 표현한다.
- 상세 패널에는 핵심 내용, 대응 전략, 답변 후보 3개와 복사 동작을 둔다.

## 6. 상호작용과 동작

- 기본 애니메이션은 사용하지 않는다.
- hover와 focus 색상 전환만 150ms 이하로 허용한다.
- 분석 시작 후 버튼을 비활성화하고 진행률을 `aria-live`로 알린다.
- 실패 항목은 오류 이유와 재시도 가능 여부를 함께 보여준다.
- 사용자가 입력한 후기와 AI 결과는 시각적으로 분리한다.
- 클라우드 제공자 선택 시 외부 전송 안내를 분석 버튼 가까이에 표시한다.

## 7. 반응형과 접근성

- 검증 폭: 375px, 768px, 1280px
- 200% 확대에서 주요 동작이 겹치거나 잘리지 않아야 한다.
- 논리적인 탭 순서와 항상 보이는 focus-visible 상태를 제공한다.
- 아이콘 단독 버튼에는 한국어 `aria-label`을 둔다.
- `prefers-reduced-motion`을 존중하며 필수 정보는 움직임에 의존하지 않는다.
- 빈 상태, 로딩 상태, 오류 상태, 부분 성공 상태를 각각 구분한다.

## 8. 허용된 디자인 부채

- MVP는 밝은 테마만 제공한다.
- 후기 200건 목록 가상화는 실제 성능 측정 후 적용한다.
- 초기 릴리스는 macOS를 우선 검증하며 다른 운영체제의 시각 검증은 후속 작업으로 남긴다.
